Drop one approved style anchor image in this directory as:

- `ember_style_anchor.png`

The local SDXL pipeline in [asset_pipeline.py](/C:/Users/msbel/projects/ember-rpg/tools/asset_pipeline.py)
will automatically use that image as the default IP-Adapter reference when
`--backend local_sdxl` is selected.

Recommended source image:

- one approved in-game item or tile exemplar
- transparent or plain background
- exact palette and outline language you want the batch to follow
- no text or UI frame

