# Implementation Index

## Scope
- Date opened: 2026-03-28
- This file is the director-mode working index for every repo-tracked change in this recovery cycle.

| File | What Changed | Why | Phase | Test |
|------|--------------|-----|-------|------|
| `docs/qa/vqr_scorecard.md` | Created baseline VQR matrix and pre-phase verdict. | The director audit requires a living scorecard before any implementation work. | Pre-phase | Manual evidence review + mandatory suite rerun |
| `docs/qa/bug_registry.md` | Created live bug ledger with fresh audit findings. | The audit found new title-flow and automation-trust failures that must be tracked immediately. | Pre-phase | Live GUI audit + automation runner reports |
| `docs/qa/implementation_index.md` | Created implementation index. | The director workflow needs a single place to find every touched file and why it changed. | Pre-phase | Manual doc review |
| `docs/qa/play_log.md` | Created play-log scaffold. | Later 50-turn and long-form passes need a canonical turn-by-turn log. | Pre-phase | Manual doc review |
| `docs/qa/demo_signoff_matrix.md` | Downgraded overstated visual and long-form gates to match fresh evidence. | The current signoff file was too optimistic for the director brief. | Pre-phase | Mandatory suite rerun + fresh GUI evidence |
| `godot-client/scripts/ui/ember_theme.gd` | Added a shared authored theme for the title screen and gameplay shell plus a title hero panel with stronger first-impression copy. | Raise UI Polish and Demo Hook without touching backend payloads. | Phase 2 slice green | `godot-client/tests/run_headless_tests.gd` green; desktop title, wizard, and resume/save-panel proof captured on `2026-03-28` |
| `godot-client/scenes/title_screen.gd` | Applied the shared theme, split `last_resume_player_id` from `last_player_id`, and now seeds a humanized resume-history line before switching into gameplay. | Fix stale-player resume behavior and stop resume handoff from showing raw backend shorthand on the first gameplay frame. | Phase 2 slice green | Headless preflight green; `title_continue_browser/20260328T032021Z` pass; `resume_and_command/20260328T035308Z` pass |
| `godot-client/scenes/game_session.gd` | Applied the shared theme, persists `last_resume_player_id`, and reuses the humanized resume-history seed after in-session campaign loads. | Keep title resume pointed at players with real resumable campaign state and make campaign reload copy consistent. | Phase 2 slice green | Headless preflight green; `resume_and_command/20260328T035308Z` pass |
| `godot-client/autoloads/game_state.gd` | Added source-level cleanup for token-like narrative text such as `resume_campaign_ok.` and a dedicated first-frame campaign-resume narrative seed. | Prevent backend shorthand from leaking directly into the first gameplay frame even when the load handoff is scene-based. | Phase 2 slice green | Headless preflight green; fresh resume desktop rerun captured |
| `godot-client/scripts/ui/narrative_panel.gd` | Humanizes token-like narrative in both live append and history load paths and now uses explicit paragraph-based scroll positioning for new blocks. | Keep fallback/system text from reading like backend identifiers and reduce wrapped-line clipping risk in the narrative pane. | Phase 2 slice green | Headless preflight green |
| `godot-client/scenes/components/narrative_panel.tscn` | Enabled wrapped RichText rendering and disabled implicit follow-scrolling in favor of script-controlled paragraph scrolling. | Make long narrative lines readable instead of silently clipping to a tail fragment. | Phase 2 slice green | Headless preflight green; `resume_and_command/20260328T035308Z` rerun inspected |
| `godot-client/scripts/world/entity_layer.gd` | Wrapped actors in body/shadow nodes and introduced bucket-based display sizing and tinting. | Improve silhouette readability and grounding without backend changes. | Phase 2 slice green | Headless preflight green; `resume_and_command/20260328T032909Z` gameplay proof |
| `godot-client/scripts/world/tile_catalog.gd` | Added multi-variant terrain atlas generation and per-position variant selection helpers. | Break the repeated-wallpaper look and lift Tile Texture Depth. | Phase 2 slice green | Headless preflight green; `resume_and_command/20260328T032909Z` gameplay proof |
| `godot-client/scripts/world/tilemap_controller.gd` | Switched tile rendering to position-based terrain variants instead of a single atlas cell per terrain type. | Ensure the new tile-depth work actually appears in the world view. | Phase 2 slice green | Headless preflight green; `resume_and_command/20260328T032909Z` gameplay proof |
| `godot-client/scripts/world/camera_controller.gd` | Increased the default gameplay zoom from `2x` to `3x`. | Make the player and nearby interaction zone readable without mousewheel adjustment. | Phase 2 slice green | Headless preflight green; `resume_and_command/20260328T032909Z` gameplay proof |
| `godot-client/scripts/world/selection_overlay.gd` | Added transient click-flash state and filled-tile feedback on selection. | Give world clicks more than a silent command emission. | Phase 2 slice green | Headless preflight green; `world_click_smoke/20260328T032606Z` pass |
| `godot-client/scripts/world/world_view.gd` | Triggers click-flash feedback when the player selects a tile. | Surface interaction acknowledgement before backend text arrives. | Phase 2 slice green | Headless preflight green; `world_click_smoke/20260328T032606Z` pass |
| `godot-client/tests/run_headless_tests.gd` | Added coverage for shared theme application, title hero panel, resume-player persistence, terrain variants, actor shadows, closer default zoom, click flash state, token cleanup, wrapped narrative rendering, and seeded resume copy. | Lock the current UI/readability slice before the next desktop rerun. | Phase 2 slice green | Headless preflight green on `2026-03-28` |
| `godot-client/tests/automation/runner.py` | Added artifact-note and artifact-diff validation so scenarios fail when they capture the wrong scene or an unchanged frame. | Turn desktop automation into fail-closed QA instead of false-positive signoff. | Phase 2 slice green | `python -m pytest godot-client/tests/automation -q` green (`24 passed`) |
| `godot-client/tests/automation/test_runner.py` | Added runner regressions for wrong viewport-note markers and unchanged-artifact detection. | Protect the new fail-closed harness behavior. | Phase 2 slice green | `python -m pytest godot-client/tests/automation -q` green (`24 passed`) |
| `godot-client/tests/automation/executors/win32_desktop.py` | Added `backspace` key mapping and fixed viewport wait so updated screenshots at the same path still count. | Let scenarios reliably clear stale text and stop timing out when F12 overwrites an existing capture path. | Phase 2 slice green | `python -m pytest godot-client/tests/automation -q` green (`24 passed`) |
| `godot-client/tests/automation/test_win32_desktop_executor.py` | Added regression coverage for the new `backspace` mapping and viewport wait behavior. | Protect Win32 field-clearing and capture synchronization. | Phase 2 slice green | `python -m pytest godot-client/tests/automation -q` green (`24 passed`) |
| `godot-client/tests/automation/scenarios/new_game_keyboard_flow.toml` | Added explicit field clearing, a real `Next` click, and changed-artifact expectations. | Make keyboard onboarding proof harder to fake and prove entry into Step 2. | Phase 2 slice green | `new_game_keyboard_flow/20260328T031956Z` pass |
| `godot-client/tests/automation/scenarios/title_continue_browser.toml` | Added explicit focus, clear, refresh, and changed-artifact expectations for the player lookup field. | Make title save-browser proof depend on a real refreshed browser state. | Phase 2 slice green | `title_continue_browser/20260328T032021Z` pass |
| `godot-client/tests/automation/scenarios/resume_and_command.toml` | Clears the player field, refreshes Chaos saves, and requires `phase2\\game` viewport markers before command validation. | Prove real title-to-game handoff and command submission. | Phase 2 slice green | `resume_and_command/20260328T032909Z` pass |
| `godot-client/tests/automation/scenarios/save_panel_smoke.toml` | Bootstraps from title `Continue`, refreshes saves, and requires gameplay viewport markers before counting save-panel proof. | Stop title-screen false positives from masquerading as in-session save coverage. | Phase 2 slice green | `save_panel_smoke/20260328T032538Z` pass |
| `godot-client/tests/automation/scenarios/world_click_smoke.toml` | Bootstraps from title `Continue`, refreshes saves, and requires gameplay viewport markers before counting world-click proof. | Stop title-screen false positives from masquerading as world-interaction proof. | Phase 2 slice green | `world_click_smoke/20260328T032606Z` pass |
| `godot-client/tests/automation/test_scenario_fixtures.py` | Added regressions that require resume-related scenarios to clear long remembered player names reliably. | Catch brittle resume-fixture mistakes before a desktop run. | Phase 2 slice green | `python -m pytest godot-client/tests/automation -q` green (`24 passed`) |
| `godot-client/scenes/components/command_bar.tscn` | Added focus-action buttons, roster buttons, and stronger authored shell framing for the bottom interaction strip. | Turn the command bar from a plain textbox row into a readable command-and-inspector surface. | Pass 1 implementation | Godot headless preflight green; fresh `50`-turn and `100`-turn desktop runs |
| `godot-client/scripts/ui/command_bar.gd` | Added persistent focus summary, visible action chips, roster population hooks, and stronger recent-order handling. | Raise click density and information architecture without backend changes. | Pass 1 implementation | Godot headless preflight green; fresh `50`-turn and `100`-turn desktop runs |
| `godot-client/scenes/components/character_panel.tscn` | Added portrait framing and clearer header hierarchy. | Make the character panel read as a game surface instead of stacked labels. | Pass 1 implementation | Godot headless preflight green |
| `godot-client/scripts/ui/character_panel.gd` | Added sprite portrait rendering, condensed stat/skill brief, role-line cleanup, and safer fallback handling. | Improve player identity and remove brittle empty-sheet behavior. | Pass 1 implementation | Godot headless preflight green |
| `godot-client/scenes/components/minimap_panel.tscn` | Reordered the survey panel so summary/intel copy is readable above the rendered map. | Make the sidebar communicate scene meaning, not just a minimap thumbnail. | Pass 1 implementation | Godot headless preflight green |
| `godot-client/scripts/ui/minimap_panel.gd` | Added scene-read and location-intel copy instead of raw minimap-only output. | Improve glanceability and location parsing. | Pass 1 implementation | Godot headless preflight green; `campaign_boot.png` / `command_100.png` desktop proof |
| `godot-client/scripts/ui/inventory_panel.gd` | Tightened empty-state and click routing so inventory entries feed the command layer cleanly. | Prevent the sidebar from feeling dead when object vocabulary is still shallow. | Pass 1 implementation | Godot headless preflight green |
| `godot-client/scripts/ui/save_load_panel.gd` | Hardened panel copy and fail-closed busy states for save/load overlays. | Keep save/load surfaces legible and truthful during graphical play. | Pass 1 implementation | Headless preflight green; `save_panel_smoke/20260328T032538Z` pass |
| `godot-client/scripts/ui/settlement_panel.gd` | Improved empty-state and summary copy for settlement status. | Make colony-lite state readable without raw backend framing. | Pass 1 implementation | Godot headless preflight green |
| `godot-client/scripts/ui/status_bar.gd` | Turned the top strip into a stronger health/location/resource dashboard. | Improve first-glance readability of the active state. | Pass 1 implementation | Godot headless preflight green; fresh `campaign_boot.png` proof |
| `godot-client/scenes/game_session.tscn` | Reordered sidebar content and promoted the survey panel higher in the stack. | Surface the most important world-reading UI earlier. | Pass 1 implementation | Godot headless preflight green |
| `godot-client/scenes/game_session.gd` | Kept command focus, focus-summary actions, and roster state in sync with live world state. | Make the shell respond like a real inspector instead of a static layout. | Pass 1 implementation | Godot headless preflight green; fresh `50`-turn and `100`-turn desktop runs |
| `godot-client/scripts/world/camera_controller.gd` | Added cluster framing and smoother readable zoom behavior on top of the closer default camera. | Keep nearby actors, props, and pressure visible instead of pinning a tiny player in a noisy map. | Pass 1 implementation | Godot headless preflight green; current-cycle desktop long-form proof |
| `godot-client/scripts/world/entity_layer.gd` | Added actor reuse, movement tweening, idle life, display sizing, and grounding treatment. | Lift silhouette readability and interaction feedback without waiting for new art. | Pass 1 implementation | Godot headless preflight green; fresh `campaign_boot.png` and `command_100.png` proof |
| `godot-client/scripts/world/selection_overlay.gd` | Added hostile, interactive, and ambient landmark overlays beyond simple click flash. | Make the map communicate pressure and affordances directly. | Pass 1 implementation | Godot headless preflight green; current-cycle desktop long-form proof |
| `godot-client/scripts/world/world_overlay.gd` | Added adapter-aware wash, vignette/depth framing, and ambient motes. | Raise atmospheric density with existing assets only. | Pass 1 implementation | Godot headless preflight green; current-cycle desktop long-form proof |
| `godot-client/scripts/world/world_view.gd` | Added focus-summary synthesis, truthful prop/tile commands, world-aware hover text, and atmosphere state wiring. | Improve object affordance honesty and raise click density. | Pass 1 implementation | Godot headless preflight green; current-cycle desktop long-form proof |
| `godot-client/scripts/world/tile_catalog.gd` | Expanded staged tile substitution, built-area surfacing, and interactive prop rendering. | Make the world read less like repeated grass wallpaper and more like authored spaces. | Pass 1 implementation | Godot headless preflight green; fresh `campaign_boot.png` and `command_100.png` proof |
| `godot-client/scripts/world/tilemap_controller.gd` | Applied staged tile substitutions in the live tilemap renderer. | Ensure tile-depth work appears in the actual game view. | Pass 1 implementation | Godot headless preflight green |
| `godot-client/autoloads/game_state.gd` | Seeded humanized resume text and removed remaining token-style narrative leakage. | Keep live gameplay from exposing backend shorthand on first frame. | Pass 1 implementation | Godot headless preflight green; current-cycle continue boot proof |
| `godot-client/scenes/title_screen.gd` | Fixed summary-step keyboard activation, busy-state behavior, and resume-player memory. | Close known title-flow UX debt before score chasing. | Pass 1 implementation | Godot headless preflight green; fresh `fantasy_ember_new_20260328T091228Z` and `scifi_frontier_new_20260328T091331Z` boots |
| `godot-client/scripts/ui/narrative_panel.gd` | Reworked narrative block retention so the newest beats stay readable instead of scrolling into clipped tail fragments. | Close the remaining first-frame and late-turn narrative readability bug. | Pass 1 implementation | Godot headless preflight green; fresh `campaign_boot.png` and `command_100.png` proof |
| `godot-client/tests/manual/campaign_visual_driver.py` | Extended the visual driver to run real `new` and `continue` flows, emit manifests, and support long-form turn counts with milestone captures. | Make `50`-turn and `100`-turn graphical proof repeatable instead of anecdotal. | Pass 1 implementation | `fantasy_ember_*` and `scifi_frontier_*` visual probe runs on `2026-03-28` |
| `godot-client/tests/run_headless_tests.gd` | Added regressions for summary activation, focus-action surfaces, actor roster, atmosphere state, ambient landmarks, and narrative block retention. | Lock the strike-pass lift before signoff. | Pass 1 implementation | Godot headless preflight green |
| `docs/qa/vqr_scorecard.md` | Rescored the build from the fresh wizard, `50`-turn, `100`-turn, and chaos evidence. | The current score moved materially and the old `3.1` snapshot was stale. | Pass 1 signoff | Fresh desktop and test evidence dated `2026-03-28` |
| `docs/qa/bug_registry.md` | Closed `DIR-005` and `DIR-007`, and recorded the remaining narrative-phrasing quality issue as `DIR-008`. | Keep QA closure aligned with current-cycle evidence instead of stale failures. | Pass 1 signoff | Fresh desktop wizard/continue runs |
| `docs/qa/play_log.md` | Added current-cycle wizard, `50`-turn, and `100`-turn desktop play milestones. | Put real visual play evidence into the canonical log. | Pass 1 signoff | Visual driver manifests and milestone screenshots |
| `docs/qa/demo_signoff_matrix.md` | Updated gate states against the current-cycle desktop and chaos evidence. | The signoff verdict must reflect the current build, not the older `3.1` slice. | Pass 1 signoff | Fresh desktop and test evidence dated `2026-03-28` |
| `docs/qa/campaign_cutover_visual_log.md` | Logged the fresh new-game, `50`-turn, and `100`-turn visual passes for both adapters. | Capture the current cycle as the new baseline for future director-mode audits. | Pass 1 signoff | Visual driver runs on `2026-03-28` |
| `docs/qa/rimworld_benchmark_report.md` | Refreshed the benchmark read from the current shell/world/readability pass. | The old benchmark narrative understated the current build. | Pass 1 signoff | Fresh desktop and chaos evidence dated `2026-03-28` |
